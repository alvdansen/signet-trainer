"""modal — the ONLY package that imports ``modal`` (App/Image/Volumes/secrets/fns).

Filled in Plan 01-03. Everything else in signet_trainer stays Modal-agnostic so the dry-run runs
locally with zero GPU and zero Modal spend (Anti-Pattern 6).
"""
