"""signet_trainer.backup — BK-01 checkpoint auto-backup.

``plan`` is a PURE, Modal-agnostic, Windows-CI module (stdlib only). The Modal sync/restore
consumers + the ``--mode restore`` gate arm land in 09.1-08 and import from here.
"""
